import { WebPlugin } from '@capacitor/core';
import type { CoreCapacitorPlugin } from './definitions';
export declare class CoreCapacitorWeb extends WebPlugin implements CoreCapacitorPlugin {
    echo(options: {
        value: string;
    }): Promise<{
        value: string;
    }>;
}
